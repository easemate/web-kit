{
  "$schema": "https://json.schemastore.org/package",
  "name": "demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf build dist .turbo",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 3003",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@easemate/web-kit": "file:../../packages/core"
  }
}
